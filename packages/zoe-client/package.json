{
	"name": "@fast-ai/zoe-client",
	"private": true,
	"version": "0.0.5",
	"main": "lib/index.js",
	"contributors": [
		"Tomas Konrady <tomaskonrady@gmail.com>"
	],
	"author": "Lundegaard a.s.",
	"license": "MIT",
	"scripts": {
		"build": "cross-env NODE_ENV=production babel src --out-dir ./lib --ignore **/*.test.js --root-mode upward",
		"prepare": "yarn build",
		"start": "cross-env NODE_ENV=development babel -w src --out-dir ./lib --ignore **/*.test.js --root-mode upward"
	},
	"dependencies": {
		"ramda": "^0.27.0",
		"ramda-extension": "^0.10.1",
		"unfetch": "^4.1.0"
	},
	"devDependencies": {
		"@babel/cli": "^7.10.1",
		"cross-env": "^7.0.2"
	}
}
